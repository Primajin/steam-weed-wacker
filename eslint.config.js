// This file exists solely for IDE ESLint integration (e.g. JetBrains WebStorm).
// It re-exports XO's eslint-adapter, which reads xo.config.js and produces
// the matching ESLint flat config automatically — no duplication required.
// See: https://github.com/xojs/xo#with-the-xo-cli-editor-integration
export {default} from 'xo/eslint-adapter';
